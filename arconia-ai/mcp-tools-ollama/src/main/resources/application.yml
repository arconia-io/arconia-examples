spring:
  ai:
    ollama:
      init:
        pull-model-strategy: when_missing
        embedding:
          include: false
      chat:
        options:
          model: llama3.2
          num-ctx: 4096
          temperature: 0.7
